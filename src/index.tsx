import React from 'react';
import { render } from 'react-dom';
import App from './App';
/**
 * The main component "App" is loaded inside root element.
 */
render(<App />, document.getElementById('root'));
